// A wrapper around smartdashboard for setting and consuming persistent values
class persistent
{
  public:
    persistent(tab std::string);
    get(value std::string);
    set(value std::string);

    set(value std::string)
    {
    }
}